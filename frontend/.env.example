# Base URL of the Flask API, INCLUDING the /api prefix (no trailing slash).
# The client appends paths like /auth/login and /public/invite/<token> directly.
# When unset, the public invite flow falls back to mock data and the dashboard
# will refuse to reach the backend.
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api
